"""
autofit — the (strictly additive, opt-in) automatic peak-fit engine.

Nothing in the existing request path imports this package; the manual-fit
path (`fitting.run_fit`, `/api/fit`) is unchanged.  See
docs/autofit/PROGRESS.md for the build log and
docs/autofit/phase1-grammar-architecture-spec-v2.md for the design spec.
"""
