You are running ROUND 16 (tenth delta recheck) of the ADVERSARIAL CODE
review of unit A0 (branch fix-local-lm-acceptance vs origin/main). Round 6
was GO x2 on the optimiser/acceptance rule; rounds 7-15 (delta: a converged
LOCAL result, and a model derived from one, is designated a STARTING POINT
everywhere it is shown, exported or saved) are archived in
docs/autofit/codex/a0_local_lm_acceptance_r{7..15}_verdict_run{A,B}.md.

Review the delta since round 6: `git diff c790c03..HEAD`. Round-15
dispositions:
1. Banner visibility → #local-model-banner now sits ABOVE the sidebar tab
   bar, outside every switchable panel (Peaks/Results/Quantify/Survey);
   structural test asserts its position precedes the tab bar and panels.
2. Local history preview → _updateLocalModelBanner() also shows (with a
   preview-specific sentence) when _historyPreview is a local snapshot,
   even over a weighted current fit; _historyPreviewSnap() and
   _historyClearPreview() refresh it. Behavioural test.
3. History restore → _historyRestoreSnap() clears tab.modelProvenance so
   the restored result governs (local result → designated via
   _isLocalFit; weighted → not).

SCOPE NOTE (unchanged): model-replacement RESULT retention is R6-7 and out
of this unit.

QUESTIONS: (a) any remaining way, within the scope note, to show, export
or save a local result or a local-derived model without a visible
designation? (b) any regression in this delta?

FORMAT: end with exactly one line:
VERDICT: GO
or
VERDICT: NO-GO — <one-sentence reason>
preceded by a numbered findings list, each rated BLOCKER / MAJOR / MINOR.
