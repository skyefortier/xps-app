You are running ROUND 5 (fourth recheck) of the ADVERSARIAL CODE review of
unit A0 (branch fix-local-lm-acceptance vs origin/main; review `git diff
origin/main..HEAD` in full plus the tests). Rounds 1-4 verdicts are
archived under docs/autofit/codex/a0_local_lm_acceptance_*verdict_run{A,B}.md
and the prompts under a0_local_lm_acceptance_*prompt.txt. Round 4 found no
BLOCKER. Verify each disposition against the code.

ROUND-4 DISPOSITIONS:

1. MAJOR the two `chi < 1e-20` exits bypassed the certificate → FIXED. The
   initial flag is now `params.length === 0 || (chi === 0 && certify())`
   and the accepted-step exit is `chi === 0 || ftol-trio || xtol`, ALL
   routed through certify(); no success path is exempt. Tests: your two
   reproductions are added verbatim ("round-4: near-zero residual with no
   data ...", "round-4: near-zero residual on the accepted-step exit ...")
   and pass the 1e-6 oracle.
2. MINOR stale derivatives after a failed gtol certificate → FIXED: that
   branch now `continue`s so the residual, Jacobian and active set are
   rebuilt at the moved point before any step is solved.
3. MINOR wording → FIXED in the code comment and CLAUDE.md: the certificate
   is a coordinate (single-parameter) feasible-descent check, not a proof
   of a local minimum along coupled directions; no exit is exempt.

State: 34 targeted tests in tests/js/local_lm_descent.test.js +
tests/js/fit_acceptance.test.js pass; full JS suite passes; 18/18 committed
Batch Fit targets converge and pass the oracle; Python and browser suites
pass on the owner's machine (not reproducible in the sandbox).

ROUND-5 QUESTIONS: (a) does any round-1..4 finding survive? (b) can you
construct a success at a point failing the stated coordinate certificate
(±1e-3 scaled single-parameter move gaining > 1e-6)? (c) anything new from
the round-4 changes?

FORMAT: end with exactly one line:
VERDICT: GO
or
VERDICT: NO-GO — <one-sentence reason>
preceded by a numbered findings list, each rated BLOCKER / MAJOR / MINOR.
