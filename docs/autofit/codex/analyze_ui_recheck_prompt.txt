Re-check of the analyze/UI/BIC-companion unit on branch
feature-autofit-stage2 (commit 5b4d099). Your review's NO-GO dispositions,
all claimed fixed:

1. (BLOCKER) /api/analyze 500-able validation → request body must be a
   JSON object (get_json(silent=True) + isinstance dict → 400); roi /
   phase / options each dict-validated → clean 400s. Pinned:
   test_analyze_non_object_bodies_are_clean_400s.
2. (BLOCKER) Find Peaks apply destructive → pushUndo() before the
   replacement (in-browser verified: undoStack 0→1 across apply); the
   review record is now DURABLE — stamped as _findPeaks on EVERY applied
   peak (peak objects round-trip project save/load with extra fields
   intact, as _rsfKey demonstrates) in addition to the in-memory tab
   record.
3. (HIGH) bic_weighted not full-k → now χ²_w + primary_fit.n_params·ln n
   (full k; docstring explains why the absent-slot heuristic must not
   shape the companion criterion).
4. (BLOCKER) filtered_dominant_alternative could flag a promoted +bfix
   candidate's free original → lineage skip (survivor names + their
   '+bfix'-stripped originals + augmented_from); pinned on the measured
   sep-0.7 case where the flag may name the genuinely-buried P3 but never
   the promoted P2 (test_promoted_bfix_original_never_flagged_as_buried).
5. (MEDIUM) fit-physics cross-check vs stale provenance prose → resolve()
   now passes the RESOLVED slots' constants (linked_offset_range /
   area_ratio of ratio-linked slots) and provenance_entries emits
   fit_physics_slot_check records + disagreement notes against them; the
   docstring claim is narrowed to the spin-orbit fields actually compared.
6. (LOW) meta option interpolation unescaped → _fpEsc on ids/labels;
   (LOW) _json_sanitize maps non-finite floats to null (pinned:
   test_json_sanitize_non_finite).

Verify each disposition. Findings with file:line, severity, failure
scenario, fix. Do not modify anything. End with exactly 'VERDICT: GO' or
'VERDICT: NO-GO (blockers: ...)'.
