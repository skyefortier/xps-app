# Codex adversarial review — negative backend areas fix (fix-negative-backend-areas, df972d6) — RUN B (2026-09-02, overnight session)

codex exec, read-only sandbox, model_reasoning_effort=high, gtimeout rails. Tokens: 153,022.
Prompt: docs/autofit/codex/area_sign_review_prompt.txt
Run A on the identical prompt/repo state also returned GO (4 MINOR findings each,
no MAJOR/BLOCKER) -> unit passes, stricter-verdict rule satisfied with GO x2.

**Findings**

1. MINOR - `abs()` is the right fix for the shipped positive-peak convention, but the backend does not fully enforce that convention for arbitrary API callers. The UI/reference paths send `amplitude_min: 0` and clamp linked ratios positive, but `_make_peak_params()` honors caller-provided `amplitude_min` and linked `area_ratio` values ([fitting.py](/Users/skyefortier/xps-app/.claude/worktrees/fix-negative-backend-areas/fitting.py:838), [fitting.py](/Users/skyefortier/xps-app/.claude/worktrees/fix-negative-backend-areas/fitting.py:862), [fitting.py](/Users/skyefortier/xps-app/.claude/worktrees/fix-negative-backend-areas/fitting.py:912)). For normal UI/autofit data, registered shapes are non-negative under positive amplitude, so `abs(trapezoid(...))` matches `autofit/engine.py`. For hostile/direct API specs, negative components should be rejected separately; the old sign was not a reliable diagnostic because it also depended on grid direction.

2. MINOR - The fixture flip is equivalent to regeneration for this change. The generators only call `refit_record()`, and `refit_record()` copies `par["area"]["value"]` after the fit; chi-square, center, fwhm, amplitude, and stderr are not functions of area sign ([autofit/parity.py](/Users/skyefortier/xps-app/.claude/worktrees/fix-negative-backend-areas/autofit/parity.py:187), [autofit/parity.py](/Users/skyefortier/xps-app/.claude/worktrees/fix-negative-backend-areas/autofit/parity.py:203), [scripts/gen_c1s_battery_fixture.py](/Users/skyefortier/xps-app/.claude/worktrees/fix-negative-backend-areas/scripts/gen_c1s_battery_fixture.py:29)). I confirmed the fixture diff has 296 negative-to-positive area pairs and no non-area changed fields.

3. MINOR - Stale negative `_backendParams.area` can still be re-serialized into saved JSON artifacts after loading an old project and saving without refit. The frontend does not read `_backendParams.area` for UI, Quantify, CSV/XLSX fit-table export, or TSV curve export; those paths recompute via `_peakArea()` / `evalPeakArray()` ([templates/index.html](/Users/skyefortier/xps-app/.claude/worktrees/fix-negative-backend-areas/templates/index.html:7075), [templates/index.html](/Users/skyefortier/xps-app/.claude/worktrees/fix-negative-backend-areas/templates/index.html:9713)). But save-fit/spectrum/project serialize peak objects wholesale, so the internal stale field can remain in `.fit.json`, `.spec.json`, or `.proj.json` even though computed `peakCurves.area` is positive ([templates/index.html](/Users/skyefortier/xps-app/.claude/worktrees/fix-negative-backend-areas/templates/index.html:8633), [templates/index.html](/Users/skyefortier/xps-app/.claude/worktrees/fix-negative-backend-areas/templates/index.html:8711), [templates/index.html](/Users/skyefortier/xps-app/.claude/worktrees/fix-negative-backend-areas/templates/index.html:8770)). This is cosmetic unless those JSON internals are treated as public result fields.

4. MINOR - The new tests pin the actual regression path adequately, but only at the integration/output level. They import this checkout’s `fitting.py`: `tests/conftest.py` inserts the current repo root, and a `python3` import probe resolved `fitting` to this worktree ([tests/conftest.py](/Users/skyefortier/xps-app/.claude/worktrees/fix-negative-backend-areas/tests/conftest.py:5), [tests/test_area_sign.py](/Users/skyefortier/xps-app/.claude/worktrees/fix-negative-backend-areas/tests/test_area_sign.py:20)). Gaussian is enough to catch the grid-direction sign bug because the changed code is shape-agnostic, but it does not test the non-negative assumption for DS/DS+G/LACX.

I did not run pytest, per your instruction and because this sandbox lacks the project test environment.

VERDICT: GO
