- **MAJOR — Retained transitive links still disable the check.** [fitting.py:1381](/Users/skyefortier/xps-app/.claude/worktrees/feature-autofit-required-refit/fitting.py:1381) inserts expressions in their existing order. Reproduced by adding retained links `9 → 3` and `10 → 9` to the redundant-anchor fixture, ordering peaks `[1, 2, 3, 10, 9]`, and requesting removal of `1`. The full fit succeeds and anchor support passes, but `required` returns `{ran:false, reason:"error", error:"NameError: name 'p9_amplitude' is not defined"}`. The page consequently bypasses the check. Plain parameters first handles direct dependencies only; create **all** retained parameters before assigning expressions, or topologically order expressions.

- **MINOR — The power floor rejects real injected anchors at high dynamic range.** [fitting.py:1393](/Users/skyefortier/xps-app/.claude/worktrees/feature-autofit-required-refit/fitting.py:1393). Reproduced using the existing fixture’s grid, background, shapes, bounds and `KW`: Graphite amplitude **10**, plus GL components `(amplitude, center, width) = (1e6, 285.1, 1.9)` and `(2300, 286.4, 1.4)`, with synthetic counts rounded to two decimals. Both fits converge; Graphite fits to amplitude **9.964**. Removing it increases χ² from **4.89e−7 to 3.64e−4**, giving **F ≈ 109,672**, yet `required:false` because the power floor is **0.111**. This is a real loss relative to the fitting residual, not numerical residue. The floor introduces a dependence on other components’ intensity and makes the page’s “fits the data as well” explanation incorrect.

The DE retained-parameter bounds probe passed, including an expression-linked child with free width. Anchor capture and stochastic seeding fixes look correct. LM and Nelder scattered-start outputs matched `main` byte-for-byte; Trust-Region showed small differences also present between repeated HEAD runs.

**Validation:** 79 Python tests and 31 JavaScript tests passed. Eight Python cases requiring filesystem writes were excluded under read-only constraints. No files changed.

**VERDICT: NO-GO.**
