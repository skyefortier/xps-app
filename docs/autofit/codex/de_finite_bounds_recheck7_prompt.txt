ROUND 8 recheck of branch fix-de-finite-bounds (HEAD; unit parent = main 35b950e). Read-only. Round 7 was NO-GO x2, all findings on the refinement-acceptance allowance (archived docs/autofit/codex/de_finite_bounds_r7_verdict_run{A,B}.md). One change since.

DISPOSITION
allowance = 1e-6*found.chisqr + 1e-11*P_raw + eps*ndata, where P_raw = sum((weights*y_raw)**2) over channels with finite x, y_sub, weights and y_raw (the channels the objective uses). Reasoning, stated in the code comment: the only way a converged least_squares refinement started from the DE solution ends above it is the local solver moving a start that sits exactly on a REQUESTED bound ~1e-10 (relative) inside it, which costs an exact fit up to ~1e-12 of the data's weighted power. Hence (1) coefficient 1e-11, not 1e-8: at 1e9 total counts the allowance is 0.01 in chi-square (run A MAJOR 1 / run B MINOR: 1 % worse is now refused — tests on a 1e6-count 1000-point Poisson spectrum and on normalised data); (2) RAW intensities, not background-subtracted: the scale does not vanish on a flat/empty region (both runs' zero-signal MAJOR — test_zero_signal_fit_is_accepted, n_perturb 0 and 3); (3) the objective's own channel mask (MINOR NaN-energy channel — test). All earlier exact-fit regressions still pass (scales 1e-3..1e6). 47 unit tests.

QUESTION FOR YOU
Both of you noted that the "materially worse accepted" cases were acceptance-gate fault injections that the unmodified solver does not produce. Judge this round on REACHABLE behaviour first: (a) any input where run_fit with differential_evolution now returns success=false for a fit that direct least_squares accepts from the same request (false failure); (b) any input where it returns success=true with a materially worse chi-square than a candidate it held (false acceptance) WITHOUT patching solver outputs. Report fault-injection-only observations separately as MINOR unless you can argue they are reachable.

Finish with findings ranked BLOCKER / MAJOR / MINOR (file:line, concrete scenario) and VERDICT: GO or VERDICT: NO-GO.
