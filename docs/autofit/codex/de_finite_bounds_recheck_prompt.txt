ROUND 2 recheck of branch fix-de-finite-bounds (HEAD; parent of the unit = main 35b950e). Read-only. Be adversarial. Round 1 was NO-GO x2 with the same three findings; your archived verdicts are docs/autofit/codex/de_finite_bounds_verdict_run{A,B}.md and the original prompt is de_finite_bounds_review_prompt.txt. Verify each disposition against the code (git diff 35b950e..HEAD -- fitting.py tests/test_differential_evolution_bounds.py CLAUDE.md), then look for anything the rework introduced.

DISPOSITIONS
1. MAJOR amplitude cap excludes valid solutions and still reports success -> the generated box is now a SEARCH limit only. run_fit: after the first DE minimisation, _active_search_sides finds generated sides the solution rests on (within 1 % of the box; an amplitude resting on a zero lower bound is NOT counted — that is a real outcome); _expand_active_search_box widens them (amplitude max tenfold; centre by one ROI span) and the fit is repeated, up to _SEARCH_BOX_EXPANSIONS = 3 times. After the perturb loop, if the final solution still rests on a generated side, success=False with a message naming the parameter(s). Your reproduction is test_generated_amplitude_limit_never_sets_the_answer (amplitude 10 000 +- 0.1 %, area equal to least_squares); the refusal is test_solution_still_on_the_search_limit_is_not_a_success.
2. MAJOR one-sided bound -> min == max -> when the generated side would not leave a real interval beside a bound the request set, it is placed one width away (ROI span for a centre, the amplitude reach for an amplitude). Tests: test_one_sided_centre_bound_outside_the_roi_keeps_a_real_interval (both of your cases), test_one_sided_dsg_centre_bound_runs_end_to_end.
3. MINOR generated ceilings echoed -> false "at lower bound" warnings and saved in _backendParams -> generated sides are never serialised: param_info min/max report None for a side in search_box (test_generated_limits_are_not_reported_as_bounds). No frontend change.
4. Site-table corrections accepted: /api/analyze and /api/analyze/start reach run_fit with options.fit_method (autofit/methods/least_squares.py:47) so they get the same box; the page stores returned bounds in _backendParams (saved with fits/projects) and _validateUncertainties reads them — both now see exactly what they saw before this unit for every method, because generated sides are not echoed.
5. NaN/inf: _finite_search_box uses finite values only and raises ValueError (HTTP 400) when x or y_sub has none (test_nonfinite_data_is_a_validation_error_not_a_nan_bound).
6. Coverage: DS+G test asserts success; linked doublet test (test_linked_doublet_runs_with_open_slave_bounds).
7. CLAUDE.md paragraph rewritten (absolute values, floor of 1, real interval, widening, success=false, not echoed; benchmark numbers labelled as measured before the widening logic).

SPECIFIC THINGS TO TRY TO BREAK
a. The perturb loop copies result.params (carrying the widened box) and runs DE again n_perturb times; a trial can come to rest on the box while the first result did not. Is the final still_active check sufficient, and can it wrongly fail a good fit (e.g. a DS+G centre legitimately at the ROI edge; an amplitude legitimately near 10x the visible maximum after widening)?
b. _active_search_sides tolerance: 1 % of (max - min). After a tenfold widening, 1 % of the box is larger than before — can a true solution at, say, 0.995 x the ORIGINAL cap be pushed into endless widening or a false failure? Can a negative amplitude_min request (open max) misbehave?
c. lmfit's DE maps parameters through bounds; does changing all_params bounds between fits leave stale internal state (e.g. `from_internal`, `init_value`) that makes the refit wrong?
d. success=False path: statistics, fitted_y and individual_peaks are still returned. The page treats success !== true as a failed fit and shows the message. Anything else consume it (autofit engine via /api/analyze treating success=False differently)?
e. Any path where search_box is non-empty for a method other than differential_evolution, or where the serialisation change alters the response for other methods.

Finish with findings ranked BLOCKER / MAJOR / MINOR (file:line, concrete failing scenario) and VERDICT: GO or VERDICT: NO-GO.
