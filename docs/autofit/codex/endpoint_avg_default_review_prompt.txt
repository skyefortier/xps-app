You are running an ADVERSARIAL review of a MEASUREMENT REPORT (no code) on
branch investigate-endpoint-avg-default (HEAD) of this repo (XPS
peak-fitting web app). The owner will decide a product default from it, so
the review target is HONESTY and METHOD: do the numbers support the
conclusions, is anything overstated, is anything missing that would change
the recommendation.

READ:
  docs/superpowers/plans/2026-09-03-endpoint-averaging-default.md   (the report)
  scripts/endpoint_avg_sensitivity.py                                (generator)
  scripts/endpoint_avg_summarize.py                                  (tables)
  docs/autofit/inventory/endpoint_avg/results.jsonl, summary.txt     (raw + printed)
  fitting.py: _apply_endpoint_averaging, shirley_background, smart_background
Verify the report's tables against results.jsonl with the summarize script
(numpy only). Your sandbox lacks lmfit, so the fits cannot be re-run; judge
the generator's method on its merits.

OWNER'S QUESTION being answered: what should endpoint averaging (n_avg)
default to? Where does one-point edge sensitivity flatten, does averaging
bias the background when the endpoint sits on a genuine slope and at what
cost, and what default follows. Today's default is 1.

REPORT'S CLAIMS TO VERIFY OR REFUTE:
- Edge sensitivity (max |Δ atomic fraction| for a 1-2 grid-point window
  edge move) flattens at n_avg 3-5: C1s 6.17 -> 1.31 -> 1.28 pp, U4f 0.23
  -> 0.19 -> 0.05 pp; non-monotonic 1.76 pp at n_avg 10 on C1s.
- Poisson scatter of the graphite fraction falls monotonically with n_avg
  (2.70 -> 0.31 pp) while mean chi2r worsens from n_avg 10 (4.87 -> 5.47
  -> 7.07): precision about the wrong level.
- Slope bias = s*(cap-1)/2 per channel; measured anchor-vs-trend biases
  match; within local noise at n 3-5, ~noise at 10 on the steepest edge
  (14.6 counts/channel), 2-4x noise at 20.
- Recommendation n_avg = 5; stated cost U4f chi2r 1.93 -> 2.10; changes
  NEW fits only (endpointAvg persists per tab).

ADVERSARIAL QUESTIONS:
1. Is a 20-point local linear fit a fair "truth" for the trend at the edge
   on these data (C1s low-BE edge has a 400-count step feature nearby; U4f
   edges sit on the satellite tails)? Could the "bias" column be an
   artifact of the trend estimate rather than of averaging? Propose a
   check if so.
2. Two tabs, one project, one lab instrument, one grid step. Is the
   generalisation to "3-15 counts/channel slopes on real lab data" and to a
   product default justified, or does the report need the other committed
   projects (B1s 46-pt windows, Cl2p, U4f on BN) before a default is set?
3. The edge-move experiment shifts the WINDOW; the owner's fragility is
   phrased as students dragging the window. Does the measurement capture
   the smart background's clamp-vs-raw behaviour correctly (the U4f chi2r
   rise at n=5 is attributed to it — is that attribution supported or a
   guess)?
4. Poisson draws were taken on rounded CPS values with np.random.poisson
   on the raw counts array; is the noise model appropriate for these data
   (dwell/scan count unknown), and does that matter for the RELATIVE
   comparison across n_avg?
5. The report says "removes ~80 % of the one-point sensitivity" and "~35 %
   of the Poisson scatter" — check the arithmetic against the tables.
6. Anything the recommendation ignores: interaction with the ROI-edge-on-
   peak-tail failure (30 % swing) which averaging cannot fix; the cap
   window//4 for very short windows; Tougaard (n_avg sets its amplitude
   anchor); the JS preview using the same convention; the auto-fit engine's
   own n_avg handling (autofit/engine.py).

FORMAT: end your final message with exactly one line:
VERDICT: GO
or
VERDICT: NO-GO — <one-sentence reason>
preceded by a numbered findings list, each rated BLOCKER / MAJOR / MINOR.
