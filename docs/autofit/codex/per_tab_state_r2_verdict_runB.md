# Codex adversarial review — per-tab state ownership (feature-per-tab-state) — round 2, RUN B (2026-09-09, reviewed at f50c9f2 / code ba739f5)

codex exec 0.153.4 (model gpt-6-astra), read-only sandbox, model_reasoning_effort=high, gtimeout rails. Tokens: 91,246.
Prompt: docs/autofit/codex/per_tab_state_r2_review_prompt.txt
Evidence at review time: node 172 pass / 0 fail / 3 known-gap todo; seven browser files (39 tests) passed together, exit-code gated; round-1 full suite on the parent: 815 passed / 7 skipped. Full pytest on this commit recorded in the round-3 header.
Round 2: NO-GO x2 — Find Peaks run/apply, debounce owner binding, fit-file explicit route, v1 import, switcher/preview/Shift+Z CLOSED (both). OPEN: BLOCKER auto-fit awaits its confirmation BEFORE capturing owner/snapshot (both); MAJOR both fit flows read method / averaging / anchors after the upload await (both); MAJOR the generic hasPeaks JSON branch applies a fit file without the owner guard (both); MAJOR batch resolves targets by id inside the loop after awaits (both); MAJOR scanner misses same-line second declarations, multiline declarators, parenthesised initialisers, regex literals corrupting depth (both); MAJOR a stored Find Peaks result is not recoverable — openFindPeaksModal never renders the active tab's stored result (B); MAJOR a pending debounced burst flushed after a later immediate action reorders history (A). MINORs: memo class wording for state / tabManager / _undoDebounce; _onLinkRatioInput mutates before its debounced push (pre-existing).

**Findings**

1. **BLOCKER — Auto-Fit still captures its owner after an await (disposition 3).** [`runAutoFitC1sGraphite`](/Users/skyefortier/xps-app/.claude/worktrees/feature-per-tab-state/templates/index.html:6882) awaits the replacement confirmation before taking `_autoFitSnapshot()` or capturing `fittingTab`. Start on A, defer confirmation, switch to B, then confirm: the operation snapshots and subsequently modifies B. An extracted-function reproduction confirmed the snapshot owner becomes B. Capture the owner before confirmation and revalidate before any mutation.

2. **MAJOR — Fit inputs remain mixed across tabs (disposition 3).** [`runFit`](/Users/skyefortier/xps-app/.claude/worktrees/feature-per-tab-state/templates/index.html:7071) reads fit method, endpoint averaging and manual anchors after upload; Auto-Fit also reads manual anchors after upload at line 6969. I reproduced a request containing A’s session/peaks and B’s averaging, method and anchors. Switching back to A before completion lets that mixed result pass `_ownerActive`. Capture the complete request inputs before upload.

3. **MAJOR — Generic JSON bypasses the fit-file ownership guard (disposition 6).** The explicit `.fit.json` branch is fixed, but [`if (hasPeaks)`](/Users/skyefortier/xps-app/.claude/worktrees/feature-per-tab-state/templates/index.html:9204) calls `_applyFitJSON(data)` without checking the captured owner. A deferred `model.json` read started on A applied to B in the reproduction. Both fit-routing branches need the same guard.

4. **MAJOR — Batch targets remain bound by persisted IDs (disposition 5).** [`runPropagation`](/Users/skyefortier/xps-app/.claude/worktrees/feature-per-tab-state/templates/index.html:10694) resolves each target inside the loop, after earlier awaits. Closing a future target and reopening its ID makes `_ownerLive(tgt)` accept the replacement object. Using controlled timers, I reproduced the reopened target’s saved model being overwritten. Capture target objects and source inputs before the first yield. The new 20 ms guard correctly prevents fitting a different active object, but deterministic timer tests are required; the additional 10 ms inter-target yield matters too.

5. **MAJOR — Scanner coverage still falls short of its stated scope (disposition 7).** [`scanModuleMutables`](/Users/skyefortier/xps-app/.claude/worktrees/feature-per-tab-state/tests/js/lib/module_state_scan.js:38) misses the second declaration in `let first=[]; let hidden=[];`, multiline declarators, and `const hidden=([]);`. A regex literal containing `{` also corrupts depth tracking and hides subsequent globals. These were reproduced and are outside the documented closure limitation. Fix token/declaration handling and add mutation tests for these cases.

6. **MAJOR — Stored Find Peaks results cannot be recovered through the promised UI (disposition 1).** Owner/payload capture, record storage and inactive-owner rendering suppression are correct. However, the new notice says to reopen Find Peaks to apply, while [`openFindPeaksModal`](/Users/skyefortier/xps-app/.claude/worktrees/feature-per-tab-state/templates/index.html:13845) always hides `fp-results` and never renders `_fpGetLast()`. The Apply button is inside that hidden container. The browser test checks storage and status text, but never follows the recovery instructions. Reopening on A must display A’s stored result.

7. **MINOR — Apply and rollback ownership checks are sound (dispositions 2–3).** `applyFindPeaks` captures the owner before both confirmations and checks identity after the asynchronous confirmation. `_autoFitRestore` correctly handles omitted/ID arguments through `snap.owner`, restores live inactive records, and rejects closed objects. Reactivation restores the record’s peaks, correction, ROI/background fields and fit display, and refreshes that record’s undo buttons. The pre-Auto-Fit undo entry remains owner-local, though rollback can leave a redundant undo step.

8. **MINOR — Debounce ordering is correct at the two named call sites (disposition 4).** [`_switchPeakShape`](/Users/skyefortier/xps-app/.claude/worktrees/feature-per-tab-state/templates/index.html:5125) calls `_pushUndoDebounced` before the family mutation; `updatePeakParam` calls it before `p[key] = value`. Thus the new immediate snapshot is pre-edit at both sites. Owner changes flush the previous burst, and closed owners are dropped. A separate pre-existing exception is `_onLinkRatioInput`, which mutates the child before calling `updatePeakParam`; that does not invalidate the cross-tab ownership fix.

9. **MINOR — The classification wording needs explicit exceptions (disposition 7).** The [memo](/Users/skyefortier/xps-app/.claude/worktrees/feature-per-tab-state/docs/superpowers/plans/2026-09-08-per-tab-state-ownership.md:37) already acknowledges `state` as an active working-copy exception. Calling it and `tabManager` “tab-independent caches” is inaccurate; likewise `_undoDebounce` contains spectrum content despite class A’s “no spectrum content” definition. Document these roles explicitly. Closure exclusions are acceptable only as a stated manual-review boundary; an async rule written in comments does not enforce them.

10. **MINOR — Remaining dispositions check out (6, 8–9).** V1 import pushes peaks/averaging history, clears redo and invalidates Find Peaks. Switcher entries hold IDs; final close clears `_historyPreview`; keyboard normalization handles actual uppercase `Z`. The inspected serializers enumerate fields and exclude runtime stacks/results, including ZIP serialization. Spectrum loaders create records, history restore reads the active record, and Organize Tabs preserves object identity.

The checkout is `f50c9f2`, a prompt-only successor to `ba739f5`; reviewed code matches the requested commit. No archived verdict files were read. Logs confirm **172 Node passes, 39 browser passes, and the parent’s 815 passed/7 skipped**. Locally, the eight focused tests passed; the full Node run encountered 12 Python parity failures caused by sandbox temporary-file restrictions. Deferred reproductions used extracted production functions with stubbed dependencies.

VERDICT: NO-GO — Async ownership remains incomplete in Auto-Fit confirmation, fit payload capture, generic fit-file loading and batch target selection.
