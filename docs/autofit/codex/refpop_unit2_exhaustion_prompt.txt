Adversarial review of unit R2 on branch feature-autofit-stage2 — element
coverage certified EXHAUSTED (commit 324b793: `git show 324b793`).
Context: docs/autofit/PROGRESS.md "Unit R2" section. Read-only sandbox.

THE CLAIM TO ATTACK: "there is nothing left to acquire under the
no-invention rule — the boundary is the archive, not the pipeline."
Evidence chain:
1. The 2026-07-03 full-table sweep probed all 103 definitional elements
   (both archived page formats via Wayback CDX): 52 OK (all recovered
   into the 51-element/78-transition machine tier), 27
   artifact-has-no-starred-value, 24 no-archive-snapshot.
2. The honest gap found before executing: cdx_snapshots() swallows
   exceptions into "no snapshot" — first-sweep CDX errors were
   indistinguishable from true absence.
3. EXECUTED 2026-07-05: the 24 no-snapshot manifest rows were cleared
   (backup kept in the session scratchpad) and re-probed once through
   the committed pipeline (scripts/acquire_nist_archive.py, its own 2s
   spacing + retries). RESULT: 0/24 recovered; every one re-confirmed
   no-snapshot. The gitignored manifest's fetch_utc rows for the 24 are
   fresh (2026-07-05) — verify if .stage9 is readable in your sandbox.
4. Committed evidence: docs/autofit/inventory/acquisition_exhaustion.json
   generated by scripts/summarize_acquisition.py (committed-generator
   rule); regeneration determinism pinned (env-gated).

ATTACK ANGLES:
- Is the exhaustion claim overreached? E.g.: 52 OK but the machine tier
  has 51 elements — WHY (find the one-element delta and check its skip
  reason is honest); are there OK elements with starred lines that were
  skip-logged for non-structural reasons (check
  elements-machine.skipped.json); could a DIFFERENT archived URL pattern
  (element pages under other query params, the compound pages
  elm_in_comp_res.asp) yield ELEMENT reference positions the claim
  ignores? (Note: compound pages are unit R3's documented future avenue
  for CHEMICAL STATES — judge whether they could also bear on element
  nominals, or whether starred element-page values remain the only
  legitimate source under the existing tier rules.)
- The aspx format finding (no evaluation markers → do not parse) is
  load-bearing for the 27 no-starred skips — is it still sound?
- tests/test_coverage_exhaustion.py: do the pins actually certify the
  claim (counts 103/52/51, only two failure classes, 51/78 tier with
  full provenance chain per transition, dark elements absent from the
  machine tier)? Would they catch a drifted regeneration, an invented
  element, a provenance-less transition? Is the curated-tier exemption
  (dark elements MAY be hand-curated later) correctly scoped?
- scripts/summarize_acquisition.py: deterministic? Any value emission
  (it must emit acquisition STATE only — statuses/reasons/counts/
  timestamps, no BEs)?

SCOPE: 324b793 touches only the generator, the committed summary, the
test file, and PROGRESS.md. No data/xps file changed (byte-identical
regeneration re-verified post-re-probe — 18 tests).

Deliver: numbered findings (BLOCKER/MAJOR/MINOR + file/line + concrete
failure scenario), then exactly "VERDICT: GO" or "VERDICT: NO-GO".
