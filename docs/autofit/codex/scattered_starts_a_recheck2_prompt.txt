ROUND 3 recheck of step (a), branch feature-scattered-starts-check (git diff main..HEAD). Read-only. Round 2 was NO-GO x2 with two shared MAJORs (archived docs/autofit/codex/scattered_starts_a_r2_verdict_run{A,B}.md; owner decisions and the site table are in scattered_starts_a_review_prompt.txt and are not up for review). Everything you cleared in rounds 1-2 is unchanged. Budget your time: a verdict is required within the run.

DISPOSITIONS
1. MAJOR background / ROI edits left evidence actionable -> validity is no longer maintained by hand in edit handlers at all. fitResult.startsModelKey = _startsModelKey(peaks, ui, ccShift, anchors): every peak field the request reads (now incl. _afAsymMin/_afAsymMax) AND the fit context (_STARTS_UI_FIELDS: bgType, bgStart, bgEnd, shirleyIter, endpointAvg, roiMin, roiMax; manual anchors; charge shift). It is COMPARED at every read: _startsLiveKey() (live peaks + tabManager._captureUI() + state.ccShift + _getManualAnchors()) for the active tab, _startsRecordKey(t) for a record (project save runs after _syncActiveToRecord). _startsIfCurrent(fr, key) is the single accessor for the panel, preview, adoption, the three save sites, the CSV/XLSX lines and _startsChosenText. History restore that brings back peaks without the matching background/ROI therefore reads as not current. The hook I had put in _invalidateFittedY is REMOVED (run A/B MINOR: a rename calls it and deleted the evidence; and it hid the stale notice).
2. MAJOR an edit made while the fit runs received a fresh key -> runFit captures ctxAtRequest = _startsLiveKey() beside peakSpecs, before the first await, and after the owner check DISCARDS the result if _startsLiveKey() !== ctxAtRequest (amber notice "…edited while the fit was running. Previous peaks and result kept."). Test on the REAL extracted runFit: centre changed to 290 and locked during the fetch -> applyBackendResult not called, previous result kept.
3. MINOR open alternative preview survived invalidation -> the preview stores altKey; _dropStaleAltPreview() runs at the top of updatePlot's preview layer and on a successful fit.
4. MINOR rename deleted counts/provenance -> fixed by (1); browser-verified through the real updatePeakParam.
5. MINOR _afAsymMin/_afAsymMax missing from the key -> added.
6. MINOR tooltip -> "that is what those starts found, no more: other starts or another method might not." (test forbids "pin it down" and similar).
7. MINOR exports omitted the recorded adoption -> "Chosen alternative" line in CSV and XLSX via _startsChosenText (only beside current evidence).
Browser re-check on :5151 through the REAL controls: rename keeps the evidence; a tab round trip keeps it; changing endpoint averaging -> stale notice and the save carries null; adoption dialog / decline / accept / one undo as before. 24 Python tests; 28 + 6 Node tests; whole JS suite green; full pytest suite re-run on HEAD.

TRY TO BREAK
a. a fit-relevant setting missing from the key (anything runFit's request reads from the DOM or state that is not in _STARTS_UI_FIELDS / anchors / ccShift — e.g. the fit METHOD: should evidence survive a method change? my position: yes, it describes the fit that produced it, and the panel does not claim otherwise — say if you disagree), or a key component that changes without a user edit (DOM value normalisation on tab switch / project load: '284.50' vs '284.5', endpointAvg number vs string, shirleyIter default, ROI auto-fill) so that fresh evidence reads as stale after a mere tab switch or reload;
b. the in-flight guard: a false discard (something that legitimately changes the key during every fit), or a bypass;
c. anything else in the diff not already cleared.

Finish with findings ranked BLOCKER / MAJOR / MINOR (file:line, concrete scenario) and VERDICT: GO or VERDICT: NO-GO.
