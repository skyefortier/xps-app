ROUND 2 recheck of step (a), branch feature-scattered-starts-check (git diff main..HEAD). Read-only. Round 1 was NO-GO x2 (archived docs/autofit/codex/scattered_starts_a_verdict_run{A,B}.md; the original prompt with the owner decisions and the site table is scattered_starts_a_review_prompt.txt — owner decisions are not up for review). Budget your time: a verdict is required within the run.

DISPOSITIONS
1. MAJOR clustering merged different chemical assignments -> the permutation rule is REMOVED: _same_solution compares component by component by the request's ids. Two interchangeable components that swapped labels are counted as a different solution of equal chi-square (not listed, since it is not lower). Test: test_solutions_are_compared_by_component_identity_never_by_permutation (your C-O / C=O case).
2. MAJOR stale alternatives stayed actionable -> the evidence is bound to the fitted model: fitResult.startsModelKey = JSON of every peak field the fit reads (_STARTS_MODEL_FIELDS: id, shape, centre, width, amplitude, every shape parameter, link fields, every fix flag), taken AFTER applyBackendResult. _startsIfCurrent(fr, peaks) is the single accessor: the panel, _altPeaks, _currentAlternative, previewAlternative, useAlternative, the three save sites and the CSV/XLSX line all go through it. After any edit / lock / shape / link change / added or removed peak / undo: the panel says the comparison no longer applies, nothing can be previewed or applied, saves and exports carry no counts. Background and ROI edits clear starts (and chosenAlternative) in _invalidateFittedY. The key is persisted with the counts so a reloaded summary is bound too. Tests: five edit kinds incl. your GL->DS + centre case and an undo; cosmetic edits (name/colour/visibility) do NOT invalidate.
3. MAJOR failed adoption left new parameters with the old result -> adoption is ATOMIC by construction: useAlternative no longer writes state.peaks; it calls runFit({startPeaks, chosenAlternative}); the REQUEST is built from startPeaks, the live model is written only by the success path (applyBackendResult, which honours locks), runFit's own pushUndo is the single undo entry (pre-apply model). Non-converged / server error / tab switch: nothing changed and the "Previous peaks and result kept" message is true. Transport failure with startPeaks: NO local fallback (the local engine would start from the live model); red notice "the alternative was not applied". Tests on the REAL extracted runFit in tests/js/fit_acceptance.test.js (four adoption tests).
4. MAJOR n_starts read after an await -> const nStarts is computed beside peakSpecs, from the same start model, before uploadToBackend; structural test pins the order.
5. MAJOR/MINOR starts vs solutions -> the server returns n_in_alternatives; the summary reads "3 found a DIFFERENT solution" / "3 found 2 DIFFERENT solutions"; persisted; table column renamed "starts here".
6. (run B MAJOR) each component's move was not shown -> every cell shows that component's own area % AND its own move from the student's start, coloured by band (the largest-move column and the confirmation rule are unchanged).
7. MINOR scatter -> amplitude keeps its sign; amplitude/width/centre are CLAMPED into the request's bounds (a thousandth of the interval off a finite wall), never redrawn; only other bounded shape parameters are redrawn in the middle 90 %. Test with your three cases.
8. Site-table corrections accepted: _invalidateFittedY (now clears the evidence); undo (key mismatch -> stale notice); adoption failure/discard paths (disposition 3); history-row highlighting (previewAlternative clears .hist-preview-active). TabManager.fromJSON's .fit.json branch clears the whole fitResult by existing design, so starts/chosenAlternative are dropped with it — unchanged, stated here.
Re-verified in a real browser on :5151 (8-JT project, fresh start built as Batch Fit builds one): table with per-component moves, preview on/off, dialog "This solution moves Adventitious 2 by −1.42 eV from where you placed it. Apply?", decline -> unchanged, accept -> chi2r 35.77 -> 15.47 with the choice recorded and ONE undo restoring the model, a parameter edit -> "The model has changed since this fit…" and preview refused. 24 Python tests, 18 + 5 Node tests, whole JS suite green; full pytest suite re-run on HEAD.

TRY TO BREAK
a. any remaining path where stale or mismatched evidence is shown, applied, saved or exported (tab switches: fitResult and peaks travel together per tab? history restore; project load; stack tabs);
b. runFit({startPeaks}): anything that still reads state.peaks for the REQUEST after my change (linked peaks / peakToBackendSpec looking up parents via getPeak on the LIVE model rather than in startPeaks!), and whether applyBackendResult on the live peaks after fitting from startPeaks can leave a field the alternative changed but the server does not return;
c. _STARTS_MODEL_FIELDS: a fit-relevant peak field missing from the key (check peakToBackendSpec), or a field in it that changes without the user editing the model (e.g. something renderPeakList/auto-fit bookkeeping writes), which would make every panel read "model has changed";
d. anything else in the diff not already cleared.

Finish with findings ranked BLOCKER / MAJOR / MINOR (file:line, concrete scenario) and VERDICT: GO or VERDICT: NO-GO.
