No BLOCKER found, but five MAJOR findings prevent approval. The site table misses invalidation through `_invalidateFittedY()`, undo/redo’s retained result, adoption failure/discard paths, and history-row highlighting.

1. **MAJOR — Chemically different assignments count as the same solution.** [fitting.py:1253](/Users/skyefortier/xps-app/.claude/worktrees/feature-scattered-starts-check/fitting.py:1253) sorts components by shape and centre, discarding identity. Example: GL “C-O” at 286.4 eV/60% and GL “C=O” at 288.0 eV/40% swap centres and fractions. This returns “same,” although the named chemical assignments reversed. It can inflate agreement or suppress a lower-χ²ᵣ alternative. **Compare by stable component ID.** Permit permutations only through explicit chemical-equivalence metadata; sharing a lineshape is insufficient.

2. **MAJOR — Stale alternatives remain actionable after model edits.** [templates/index.html:7261](/Users/skyefortier/xps-app/.claude/worktrees/feature-scattered-starts-check/templates/index.html:7261) checks only IDs and component count. Changing a centre, shape, lock, or link passes. I reproduced changing C-O to Gaussian at 290 eV: `_altPeaks()` accepted the old GL alternative and applied its parameters to the Gaussian. The advertised χ²ᵣ and fractions therefore describe a different model. Meanwhile, `_invalidateFittedY()` retains `starts`; undo restores peaks while retaining the latest result and its counts. Bind the diagnostic to the fitted model/request and reject or visibly invalidate mismatches, including in saves and exports.

3. **MAJOR — Failed adoption leaves alternative peaks paired with the previous result.** [templates/index.html:7309](/Users/skyefortier/xps-app/.claude/worktrees/feature-scattered-starts-check/templates/index.html:7309) commits peaks before awaiting `runFit()`. With the actual extracted functions and a `success:false` response, the centre changed from 285 to 285.3, the old result remained, and the notification falsely said “Previous peaks and result kept.” Switching tabs during adoption similarly discards the refit after the source model was changed. A successful local fallback also loses `chosenAlternative`. Make adoption an owner-bound transaction with consistent failure/discard handling and provenance on successful fallback. One undo restores the pre-apply **model**, but does not restore its `fitResult`; that existing limitation remains.

4. **MAJOR — Individual component displacements are never shown.** [templates/index.html:7245](/Users/skyefortier/xps-app/.claude/worktrees/feature-scattered-starts-check/templates/index.html:7245) renders fractions and only the largest displacement, despite receiving every component’s shift. If two components move −1.47 and +1.20 eV, the second relocation is invisible in the comparison. This misses the explicit requirement to show **each component’s** movement from the student’s start. Add named per-component displacements; retain the largest displacement for the confirmation rule.

5. **MAJOR — The check’s trigger is read from mutable state after upload.** [templates/index.html:7375](/Users/skyefortier/xps-app/.claude/worktrees/feature-scattered-starts-check/templates/index.html:7375) reads `state.peaks` after `await uploadToBackend()`, although `peakSpecs` was captured beforehand. I reproduced a request containing two peaks but `n_starts: 0`. Switching from a multi-component tab to a single-component tab during upload produces this mismatch; returning before completion can accept an unchecked fit. Capture the count before the await, alongside the other request inputs.

6. **MINOR — The summary confuses solution count with start count.** [templates/index.html:7216](/Users/skyefortier/xps-app/.claude/worktrees/feature-scattered-starts-check/templates/index.html:7216) says “one start found” whenever there is one alternative cluster. With all three starts reaching that cluster, it still says “one start”; saving and reloading preserves the incorrect wording. Count starts separately from clusters. The saved counts already allow deriving the number reaching alternatives.

7. **MINOR — Scatter changes amplitude sign and can greatly exceed the stated width perturbation.** [fitting.py:1206](/Users/skyefortier/xps-app/.claude/worktrees/feature-scattered-starts-check/fitting.py:1206). With an open amplitude floor, −100 became +102.63 in a probe. A width of 0.1 bounded by `[0.1, 15]` became 0.845 because of the 5% interior margin—8.45× rather than at most 1.5×. Preserve nonzero amplitude sign and distinguish clipping amplitude/width/centre draws from drawing shape parameters inside the middle 90%. These examples remain within request bounds; they distort the specified search.

8. **MINOR — Tooltip wording overclaims what agreement establishes.** [templates/index.html:7190](/Users/skyefortier/xps-app/.claude/worktrees/feature-scattered-starts-check/templates/index.html:7190) says agreement means “the data pin it down from those starts.” Three starts can converge to the same unresolved decomposition. Replace that inference with the observed count, consistent with the owner’s wording requirement.

9. **MINOR — Alternative preview leaves a history snapshot highlighted.** [templates/index.html:7287](/Users/skyefortier/xps-app/.claude/worktrees/feature-scattered-starts-check/templates/index.html:7287) replaces `_historyPreview` without clearing `.hist-preview-active`. Preview a history snapshot, then an alternative: the chart shows the alternative while the history row still identifies the snapshot as previewed. Clear the old highlighting when replacing the preview.

No server-side mutation path changing THE FIT was found. The scatter copies parameters, lmfit deep-copies fit inputs, and the global methods skip the check. Byte-identity probes passed for LM, Nelder–Mead, and Trust-Region on the tested fixture; this does not establish universal Trust-Region reproducibility. Native `confirm()` matches existing conventions, and its strict `> 1 eV` condition is correct for fresh alternatives. Summary-only reloads render safely, and CSV/XLSX omit the line when the check did not run.

Validation: **54 targeted JavaScript tests passed**, plus **12 Python test functions executed directly without filesystem writes**. Normal pytest execution was blocked by the read-only temporary-directory restriction; API tests were not run. Important missing coverage includes real adoption failure/fallback/tab switching, same-ID model edits, undo/result mismatch, multiple starts in one alternative cluster, and the planned committed-real-data regression. The existing label-swap test actually enforces finding 1.

**VERDICT: NO-GO**
