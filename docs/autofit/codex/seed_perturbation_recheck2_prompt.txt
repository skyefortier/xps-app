ROUND 3 recheck of branch fix-seed-perturbation (HEAD; parent = main). Read-only. Round 2 verdicts archived: docs/autofit/codex/seed_perturbation_r2_verdict_run{A,B}.md (run B NO-GO on one MAJOR; run A no BLOCKER/MAJOR). You cleared dispositions 1, 3, 4 of round 1; they are unchanged.

DISPOSITIONS
1. (run B MAJOR / run A MINOR) inactive shape settings still changed the fit (fix_gl_ratio on a Gaussian: area fraction 8 % -> 53 %) -> your suggestion adopted: the seed now hashes the EFFECTIVE parameters the optimiser is given. _request_seed(x, counts, shapes, params, ...) is called in run_fit AFTER the composite model is built and hashes each component's lineshape (fitting order) and, for every lmfit parameter, [name, value, min, max, vary, expr] — plus data, background settings (anchors sorted), method, solver options, n_perturb. The _SEED_SPEC_KEYS whitelist, its drift test and the None-dropping are gone; null vs absent now differ exactly when the fit treats them differently (amplitude_min: null opens the floor -> different seed). Tests: test_a_setting_that_is_inert_for_the_shape_does_not_change_the_fit (your case, byte-identical LM result for two peak positions), test_the_seed_is_a_pure_function_of_what_the_optimiser_is_given (inert shape params, cosmetic fields, spelling, anchors order -> same seed; eight real changes incl. amplitude_min: null and an ACTIVE fix_gl_ratio -> eight different seeds).
2. (run B MINOR) the draw pin did not observe production -> test_the_perturbation_draws_run_fit_makes_are_pinned recovers the draws from run_fit itself (LM, caller seed 7: perturbed start / first solution) and pins all eight; test_another_seed_gives_other_draws. Replacing the production generator now fails the pin.
3. (both MINOR) stale statements -> the run_fit comment says identical DRAWS, not an identical fit; the test docstring attributes the arithmetic to the BLAS in use (Apple Accelerate here); CLAUDE.md describes a deterministic perturbation base as a MITIGATION that does not make results identical (run A's experiment), and no longer claims absent == null.
35 tests in the file; full suite re-run on HEAD.

TRY TO BREAK
a. Two requests the fit treats identically that still get different seeds through the effective-parameter hash (e.g. parameter ORDER differences from peak order, a fixed parameter's irrelevant bounds, float formatting of inf/-inf, expr strings that differ textually but not semantically), and whether any of those is page-reachable and material.
b. Two requests the fit treats DIFFERENTLY that share a seed for a structural reason (not a 32-bit collision).
c. The seed is now computed after the model is built: any path where random_seed is referenced before assignment, or where an early exception changes the error type the API returns (ValueError -> 400 vs RuntimeError -> 422).
d. Anything else in git diff main..HEAD not already cleared.

Finish with findings ranked BLOCKER / MAJOR / MINOR (file:line, concrete scenario) and VERDICT: GO or VERDICT: NO-GO.
