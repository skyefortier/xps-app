# Codex adversarial review — shirley_linear safe de-list (fix-shirley-linear-delist) — round 1, RUN A (2026-09-08, reviewed at 03c20af / code 8b93123)

codex exec 0.153.4 (model gpt-6-astra), read-only sandbox, model_reasoning_effort=high, gtimeout rails. Tokens: 91,306.
Prompt: docs/autofit/codex/shirley_linear_delist_review_prompt.txt
Evidence at review time: browser tests 3/3 + unit 2/2 on the branch (watched failing on main); node 155 pass / 0 fail / 3 known-gap todo; full pytest on the branch worktree 763 passed, 7 skipped (6 env-gated + the local-only {7,8}* datasets absent from the worktree).
Round 1: GO x2 — six MINORs each, no BLOCKER/MAJOR. Dispositions: note wording tightened to 'the background the backend fit subtracts' and the helper comment widened to name batch propagation as a second route (both runs); the history-restore-without-note limitation and the batch-propagation-as-new-use point are accepted as out of the dropdown-only scope and logged; cross-engine setter ordering left as-is (standards-correct per both runs).

**Findings**

No BLOCKER or MAJOR found under the stated dropdown-only de-listing scope. Actual HEAD is `03c20af`, a documentation-only successor to `8b93123`; the three reviewed files are unchanged. This was a read-only review; I did not run tests.

1. **MINOR — Tests miss event wiring and numerical regressions.** The [browser tests](/Users/skyefortier/xps-app/.claude/worktrees/fix-shirley-linear-delist/tests/test_browser_shirley_linear_delist.py:172) would reject CSS-only hiding, deletion of the option, or removal of `_syncLegacyBgOption()` from `_onBgTypeChange()`. However, removing the select’s `onchange` attribute would pass: the test invokes the handler directly. Replacing the legacy calculation with another finite, nonzero background could also pass; the backend test checks success and length, while the source checks establish only textual presence. Use an actual dropdown change and add a saved-file round trip with a pinned legacy background. The inspected implementation retains the event wiring and calculation.

2. **MINOR — History can restore a legacy curve without restoring its warning.** All identified background-type writers are covered: v1 fit loading reaches `_restoreUI` through `fromJSON`; spectrum loading calls it explicitly; project/ZIP loading and normal batch activation reach it through `activateTab`. `_autoFitRestore` writes endpoints, ROI and charge-correction fields, **not** `bg-type`; undo/redo restore peaks only. However, [history restore](/Users/skyefortier/xps-app/.claude/worktrees/fix-shirley-linear-delist/templates/index.html:12485) restores `fitResult` without background settings. Reproduce by fitting a legacy tab, switching to Shirley, then restoring that snapshot: the frozen legacy curve returns while the selector remains Shirley and the warning stays hidden. This is an existing history-provenance limitation exposed by the new warning, not a missed background-value setter.

3. **MINOR — Batch propagation remains a route to new legacy usage.** [Propagation](/Users/skyefortier/xps-app/.claude/worktrees/fix-shirley-linear-delist/static/js/batch_propagation.js:24) copies `shirley_linear` into previously nonlegacy targets. Activation displays the option and note correctly. I consider this acceptable under the explicit instruction to remove it **from the background-type dropdown**, while preserving existing workflows. It does not implement a global prohibition on new usage. Consequently, “only shown for saved files that already use it” is too narrow: propagated targets also show it. A broader prohibition would require a separate propagation policy; silently substituting another background would be unsafe.

4. **MINOR — Cross-engine execution remains unverified; the setter ordering is valid.** The [HTML Standard’s value setter](https://html.spec.whatwg.org/multipage/form-elements.html#dom-select-value) selects the first matching option without filtering on `disabled` or `hidden`, and explicitly permits programmatic changes to disabled options’ selectedness. [WebKit’s implementation](https://raw.githubusercontent.com/WebKit/WebKit/main/Source/WebCore/html/HTMLSelectElement.cpp) likewise matches the value and sets the selected index. I found no evidence of the proposed Firefox/Safari refusal, but did not execute either engine. The most defensive ordering is to enable/unhide based on incoming `ui.bgType`, assign the value, then synchronize the note; current code assigns first. That is not a demonstrated defect. No traced application writer leaves a lasting selected-but-hidden legacy option; native display of that hypothetical state was not verified.

5. **MINOR — Warning is scientifically accurate, with a small wording improvement.** The [task-4 report](/Users/skyefortier/xps-app/.claude/worktrees/fix-shirley-linear-delist/docs/superpowers/plans/2026-09-02-task4-background-twin-parity.md) supports “can differ”: descending-grid JS and Python calculations diverged substantially, and backend-fitted results retain the JS background for display. Restored projects preserve those arrays; spectrum files can recompute the unchanged JS preview. Local fits—including batch propagation—use the JS background themselves, so this particular mismatch is not universal. “The background the **backend fit** subtracts” would be more precise. Preserving the old behavior also preserves the known disagreement, as intended.

6. **MINOR — No option-enumeration or comment regression found.** The [tooltip code](/Users/skyefortier/xps-app/.claude/worktrees/fix-shirley-linear-delist/templates/index.html:10410) uses `select.options[select.selectedIndex]`; hidden options remain in that collection and comments are excluded. I found no clone or custom option-list builder for this select. The batch modal enumerates tabs, not background options. The inserted comment therefore does not disturb option indexing or tooltip lookup.

VERDICT: GO
