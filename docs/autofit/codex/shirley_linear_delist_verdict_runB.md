# Codex adversarial review — shirley_linear safe de-list (fix-shirley-linear-delist) — round 1, RUN B (2026-09-08, reviewed at 03c20af / code 8b93123)

codex exec 0.153.4 (model gpt-6-astra), read-only sandbox, model_reasoning_effort=high, gtimeout rails. Tokens: 103,724.
Prompt: docs/autofit/codex/shirley_linear_delist_review_prompt.txt
Evidence at review time: browser tests 3/3 + unit 2/2 on the branch (watched failing on main); node 155 pass / 0 fail / 3 known-gap todo; full pytest on the branch worktree 763 passed, 7 skipped (6 env-gated + the local-only {7,8}* datasets absent from the worktree).
Round 1: GO x2 — six MINORs each, no BLOCKER/MAJOR. Dispositions: note wording tightened to 'the background the backend fit subtracts' and the helper comment widened to name batch propagation as a second route (both runs); the history-restore-without-note limitation and the batch-propagation-as-new-use point are accepted as out of the dropdown-only scope and logged; cross-engine setter ordering left as-is (standards-correct per both runs).

**Findings**

No BLOCKER or MAJOR found. Reviewed the requested files at `8b93123`; current HEAD `03c20af` adds only the review prompt. I did not run tests or independently verify the full-pytest result.

1. **MINOR — Cross-engine execution remains untested, but the setter concern is not supported.** The [HTML standard](https://html.spec.whatwg.org/multipage/form-elements.html#dom-select-value) selects the first matching option without filtering `disabled` or `hidden`, and explicitly permits programmatic selection of disabled options. [WebKit](https://raw.githubusercontent.com/WebKit/WebKit/main/Source/WebCore/html/HTMLSelectElement.cpp) and [Gecko](https://raw.githubusercontent.com/mozilla-firefox/firefox/main/dom/html/HTMLSelectElement.cpp) implement value matching without those exclusions. I found no evidence of the proposed refusal. Defensive ordering would be: enable/unhide based on the incoming `ui.bgType`, assign `.value`, then synchronize. Current code assigns first; that is standards-correct, although I cannot certify every shipped browser version.

2. **MINOR — No application value-write bypass found; history has a separate warning limitation.** `_restoreUI` assigns the value and immediately synchronizes. The v1 fit loader calls it after updating `active.ui`; spectrum JSON calls it after merging saved settings; project/ZIP loading reaches it through activation. Batch also updates records and then activates. Undo/redo restore peaks only; `_autoFitRestore` restores background endpoints, **not background type**. History restores peaks and `fitResult`, not `bgType`. Thus no identified writer leaves the legacy option selected but hidden. A hypothetical unsynchronized assignment still retains the DOM value; native label rendering should not be assumed identical across engines. Separately, restoring a historical legacy fit after choosing another background can display its frozen legacy curve without the note. That is an existing history/settings mismatch, outside the stated “loads with it selected” requirement.

3. **MINOR — Batch propagation permits new use, consistent with the narrower dropdown decision.** [Propagation copies `bgType` unchanged](/Users/skyefortier/xps-app/.claude/worktrees/fix-shirley-linear-delist/static/js/batch_propagation.js:20), so a legacy source can apply this method to previously nonlegacy targets. Activation shows their notes. I accept this under the explicit instruction to remove selection **from the dropdown** while retaining legacy behavior. It would violate a broader prohibition on all new applications. The helper comment implying saved-file restoration is the only route is therefore too narrow.

4. **MINOR — The warning accurately describes the finding, with a useful qualification.** Task 4 demonstrates opposite-side correction placement on descending grids and confirms that backend-fit display uses frozen JS background arrays. Restored projects can retain those arrays; spectrum restores recompute through the unchanged JS route. “Can differ” is appropriate: local fitting—including batch propagation—uses the JS background itself, so that particular discrepancy does not apply to every fit. “Backend fit” would make the warning more precise. “Exactly as before” means compatibility, not scientific correctness.

5. **MINOR — No option-enumeration or comment regression found.** The tooltip reads `select.options[select.selectedIndex]`; hidden options remain in that collection, and comments never enter it. Keeping the option preserves existing option indices. Batch UI enumerates tabs, and the identified `cloneNode` call clones an unrelated input. Neither introduces a selection bypass.

6. **MINOR — Tests catch the proposed simple mistakes, but do not establish full compatibility.** Assuming all five execute, CSS-only hiding fails the `hidden`/`disabled` assertions; omitting synchronization from `_onBgTypeChange` fails the switch-away test; deleting the option fails presence and restore assertions. However, removing the actual HTML `onchange` binding can pass because the test calls the handler directly. Routing legacy backgrounds to another finite, nonzero implementation can also pass. The “saved file” test constructs a tab rather than loading, saving, and reopening a file. Add a real selection-event test and a legacy-file round trip with numerical background assertions when strengthening coverage.

VERDICT: GO
