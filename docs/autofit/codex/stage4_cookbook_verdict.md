**Findings**

- **BLOCKER** [autofit/methods/ic_model_comparison.py](/Users/skyefortier/xps-app/autofit/methods/ic_model_comparison.py:94), [autofit/regions/cl2p.py](/Users/skyefortier/xps-app/autofit/regions/cl2p.py:33): Cl 2p conditional provenance is not modeled, only inferred from boundary hits. If fixed `Cl0_doublet` wins, the CONDITIONAL 0.5 ratio is silently forced with `conditional=False`. If relaxed ratio wins at an interior value like 0.53, it can also be reported clean because no boundary hit fires. Fix by adding explicit candidate/slot provenance for CONDITIONAL constants/findings and surfacing it independent of boundary status.

- **BLOCKER** [autofit/engine.py](/Users/skyefortier/xps-app/autofit/engine.py:1024): the decisive override makes BIC* a hard decision rule despite spec §6 saying BIC* is heuristic and a relative tie-breaker only. Near ΔBIC*=10, tiny optimizer/multistart/background changes flip clean-vs-conditional semantics. On processed XPS data, Kass/Raftery calibration is not defensible for absent-slot-adjusted BIC*. Fix by demoting this to an ambiguity/alternative path unless confirmed by reduced-model refits, profile likelihood, bootstrap/CV, or later Bayesian evidence.

- **BLOCKER** [autofit/engine.py](/Users/skyefortier/xps-app/autofit/engine.py:400): the Cl 2p relaxed winner is boundary-pegged at the ratio bound, but the BIC* comparison still treats that ratio as an ordinary free interior parameter. A boundary-constrained model changes the effective parameter count and invalidates the “decisive” BIC* comparison as implemented. Fix by fitting explicit fixed-boundary variants or treating boundary-pegged winners as conditional alternatives, not BIC*-decisive replacements.

- **MAJOR** [autofit/engine.py](/Users/skyefortier/xps-app/autofit/engine.py:1019): when the override fires, the code replaces clean survivors with the entire `conditional_pool`, not just the decisive conditional model. A bad boundary-limited model can become a survivor merely because another conditional model beat clean by >10. Fix by preserving clean survivors as alternatives and promoting only models meeting the override criterion, with an explicit reason.

- **MAJOR** [autofit/methods/ic_model_comparison.py](/Users/skyefortier/xps-app/autofit/methods/ic_model_comparison.py:95): the user-facing message says “no candidate passed plausibility cleanly” even for the decisive-override path where clean candidates did pass and were replaced. That misreports the key scientific choice. Fix with a `conditional_reason` enum: `no_clean_survivor` vs `decisive_override`.

- **MAJOR** [tests/autofit/test_b1s_cl2p_parity_gates.py](/Users/skyefortier/xps-app/tests/autofit/test_b1s_cl2p_parity_gates.py:82): the Cl 2p gate is winner-dependent and tautological. It accepts fixed-ratio or relaxed-ratio behavior after observing the winner, so a regression that stops surfacing the documented relaxed CONDITIONAL winner can pass if fixed ratio still beats expert χ². Fix this gate to assert the known anchor result directly: relaxed winner, `conditional=True`, ratio violation surfaced, and candidate table records the fixed-vs-relaxed evidence.

- **MAJOR** [autofit/regions/b1s.py](/Users/skyefortier/xps-app/autofit/regions/b1s.py:38), [autofit/engine.py](/Users/skyefortier/xps-app/autofit/engine.py:552): B 1s low/mid split at exactly 187.9 creates an inclusive shared boundary. A component at 187.9 matches both windows and role assignment falls through to nearest/window ordering; symmetric overlapping peaks can swap or collapse onto the boundary while still appearing persistent. Fix with unordered low/mid pair handling, role-swap detection, a minimum-separation/proximity flag, or non-overlapping windows with an explicit ambiguity band.

- **MINOR** [autofit/regions/b1s.py](/Users/skyefortier/xps-app/autofit/regions/b1s.py:4), [tests/autofit/test_b1s_cl2p_parity_gates.py](/Users/skyefortier/xps-app/tests/autofit/test_b1s_cl2p_parity_gates.py:58): comments cite the B-C/B-B conflict as PROGRESS discrepancy #7, but #7 is Cl 2p ratio and the B 1s assignment conflict is #8. Fix the references; the position-neutral role implementation itself satisfies the hard rule.

- **Constant audit:** executable constants in `b1s.py`/`cl2p.py` are commented as UNVERIFIED, CONDITIONAL, or derived from those values. The bigger defect is that these verdicts are comments only; runtime output cannot distinguish “clean physics” from “uses CONDITIONAL-cited constants.”

Verification note: I did not modify files. Scoped pytest could not run under the read-only sandbox because pytest could not create a temp capture file.

VERDICT: NO-GO (blockers: Cl 2p conditional provenance; decisive-override/BIC* validity; boundary-pegged BIC comparison; tautological Cl 2p gate)