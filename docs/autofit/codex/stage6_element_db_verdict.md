**Findings**

**BLOCKER** [tests/test_expand_coverage.py](/Users/skyefortier/xps-app/tests/test_expand_coverage.py:23): the independent artifact oracle is stale. `EXPANSION` still lists only the original 18 expansion records, while provenance has 51 acquisition records. The “literally in committed artifact”, SHA, and provenance-complete tests at [line 75](/Users/skyefortier/xps-app/tests/test_expand_coverage.py:75) and [line 94](/Users/skyefortier/xps-app/tests/test_expand_coverage.py:94) only iterate those 18; the new 33 are only covered by the coarse count at [line 118](/Users/skyefortier/xps-app/tests/test_expand_coverage.py:118) and the self-reported `agent_cross_checked` flag check in [tests/test_machine_tier.py](/Users/skyefortier/xps-app/tests/test_machine_tier.py:229).  
Failure scenario: `Rh-3d5/2` or `Mg-2p3/2` could be changed in `elements-machine.json`, provenance, and `agent_crosscheck.json` consistently while not being a `<b>*</b>` artifact value; the current independent parser test would not look at it.  
Fix: derive the acquisition set from `elements-machine.provenance.json` and run the independent `<tr>/<td>` + `<b>*</b>` parser and SHA check for every `p.get("acquisition")`, not a hard-coded subset.

**BLOCKER** [tests/test_machine_tier.py](/Users/skyefortier/xps-app/tests/test_machine_tier.py:193): internal duplicate testing is exact-orbital only, not subshell-level. The generator intends to prevent expansion records sharing `(element, subshell)` with tiers-machine records at [scripts/gen_machine_tier.py](/Users/skyefortier/xps-app/scripts/gen_machine_tier.py:383) and [line 420](/Users/skyefortier/xps-app/scripts/gen_machine_tier.py:420), but the test would miss `X-4d` plus `X-4d5/2`.  
Failure scenario: a regression in `tiers_machine_sub` lets a bare `3p` expansion line coexist with a tiers-driven `3p3/2`; [line 198](/Users/skyefortier/xps-app/tests/test_machine_tier.py:198) sees distinct orbitals and passes.  
Fix: add a machine-internal `(element, _subshell(orbital))` uniqueness test, or explicitly assert every `already-machine-tier` skip corresponds to an otherwise-emittable expansion line whose subshell is already present.

**MAJOR** [tests/test_machine_tier.py](/Users/skyefortier/xps-app/tests/test_machine_tier.py:211): archive provenance pattern checks are too loose and do not verify artifact bytes. They only require a web.archive prefix plus substrings and a 64-char SHA at [lines 219-225](/Users/skyefortier/xps-app/tests/test_machine_tier.py:219).  
Failure scenario: malformed archive URL, wrong element query, mismatched `archive_snapshot_timestamp`, or SHA not matching the tracked artifact passes for the 33 records not covered by `test_expand_coverage`.  
Fix: strict-regex `https?://web.archive.org/web/<14 digits>id_/https?://srdata.nist.gov(:80)?/xps/query_all_dat_el.asp?elm1=<element>`, compare timestamp to `archive_snapshot_timestamp`, and hash the tracked artifact for every acquisition record.

**Notes**

My inline artifact audit found no current emitted-data mismatch: all 78 provenance records had local artifacts, matching SHA, a nominal value found by the committed parser, and an independent `<b>*</b>` parse; no multiple-star ambiguity appeared in this artifact set. `build()` also regenerated byte-identical machine/provenance/skipped JSON. `fit-physics.json` has 98 entries, 78 machine entries, no machine `spin_orbit`, and no statistical ratio on an `s` line.

I could not run pytest in this read-only sandbox because pytest capture requires a writable temp directory.

VERDICT: NO-GO (blockers: stale expansion artifact oracle for 33 new records; machine-machine subshell overlap invariant unpinned)
VERDICT: NO-GO (blockers: stale expansion artifact oracle for 33 new records; machine-machine subshell overlap invariant unpinned)
