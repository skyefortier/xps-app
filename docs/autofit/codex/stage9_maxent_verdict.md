.py:26](/Users/skyefortier/xps-app/autofit/methods/max_entropy.py:26), [max_entropy.py:118](/Users/skyefortier/xps-app/autofit/methods/max_entropy.py:118): the update is not a MaxEnt Gaussian-likelihood ascent with entropy prior. For Gaussian likelihood, the data gradient is `K^T(y-Kf)/sigma^2`, and entropy adds `-alpha*(log(f/m)+1)`. Here `sigma^2` cancels and no entropy gradient appears; this is closer to a damped exponentiated ISRA/RL-like NNLS deconvolution step. Failure scenario: the method can pass the χ² target while selecting an unnecessarily low-entropy, artifact-sharpened solution, despite claiming “maximizes entropy.” Fix: implement a real constrained MaxEnt solve or entropy-regularized objective with explicit `alpha`/Lagrange handling and line search, or rename/relabel this as RL/ISRA-style resolution enhancement and remove MaxEnt claims.

- **MAJOR** [max_entropy.py:92](/Users/skyefortier/xps-app/autofit/methods/max_entropy.py:92), [max_entropy.py:113](/Users/skyefortier/xps-app/autofit/methods/max_entropy.py:113), [max_entropy.py:142](/Users/skyefortier/xps-app/autofit/methods/max_entropy.py:142): the floor shift makes the reported χ² apply to `Kf` versus `y-min(y)`, not necessarily to the emitted `sharpened_spectrum = f + floor`. Interior constant offsets are mostly harmless, but `mode="same"` zero-padding loses mass at boundaries, and a single negative noise outlier can set an arbitrary global offset that changes entropy/preconditioning. If `floor < 0`, the restored output can also go negative, violating the documented `f >= 0` interpretation. Fix: model an explicit additive offset/baseline and compute/report χ² on `Kf + offset` against original `y`; expose the offset separately; use boundary-normalized/cropped/reflect convolution or warn/crop edge regions.

- **MAJOR** [max_entropy.py:100](/Users/skyefortier/xps-app/autofit/methods/max_entropy.py:100), [max_entropy.py:113](/Users/skyefortier/xps-app/autofit/methods/max_entropy.py:113): the `/sqrt(6)` factor is mathematically correct only for iid white Gaussian noise in the second difference. Processed XPS data are often correlated and peak curvature contaminates `d2`, so σ can be badly biased. Overestimated σ silently stops early and under-sharpens; underestimated σ drives extra sharpening/noise artifacts until target or `max_iter`. The `noise_sigma_source` flag is useful but not enough to communicate that σ controls sharpening severity. Fix: strengthen payload warning, prefer/require repeat-sweep or user σ for production, and add sensitivity diagnostics or at least report “σ-estimated stopping is uncalibrated.”

- **MINOR** [max_entropy.py:106](/Users/skyefortier/xps-app/autofit/methods/max_entropy.py:106): the symmetric-kernel transpose claim is mostly OK for the built odd Gaussian kernel; zero-padded `same` convolution remains a symmetric linear operator. But it is not flux-preserving at spectrum boundaries, so flat spectra are depressed near edges and edge artifacts can affect stopping. Fix: document/crop boundary regions or use a boundary convention that preserves constants.

- **MINOR** [max_entropy.py:141](/Users/skyefortier/xps-app/autofit/methods/max_entropy.py:141): `relative_entropy_to_flat` is actually negative KL divergence to a flat distribution, not conventional nonnegative relative entropy. It is meaningful as an internal sharpness/nonuniformity scalar only. Fix: rename to `negative_kl_to_flat` or report both `shannon_entropy` and `kl_from_flat`.

- **MAJOR** [test_max_entropy.py:66](/Users/skyefortier/xps-app/tests/autofit/test_max_entropy.py:66): the artifact test only bounds excursions in a left flat region relative to the main feature. A shoulder artifact, peak split, inter-peak false structure, or boundary artifact can pass while being scientifically misleading. Fix: add pins for local-max count/locations in the feature window, shoulder/inter-peak artifact bounds, both flat tails, reconvolution χ² of the emitted/restored model, and a no-new-peaks-above-`k*sigma` invariant.

- **MINOR** [test_max_entropy.py:90](/Users/skyefortier/xps-app/tests/autofit/test_max_entropy.py:90), [test_max_entropy.py:101](/Users/skyefortier/xps-app/tests/autofit/test_max_entropy.py:101): kernel-required and determinism are basically pinned, but the tests do not cover zero/NaN/too-wide kernels, estimated-σ provenance, or the broad-kernel case where `np.convolve(..., mode="same")` can return the kernel length instead of the spectrum length. Fix: add validation tests for invalid/nonfinite/broader-than-grid kernels and the estimated σ path.

I could not run the tests here because `pytest` is not installed in the available Python environment.

VERDICT: NO-GO (blockers: update is not MaxEnt/entropy-regularized Gaussian ascent)
