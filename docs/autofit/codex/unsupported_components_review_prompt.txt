Review step (b) of a three-step unit: branch feature-unsupported-components, git diff main..HEAD (fitting.py, templates/index.html, tests/test_component_support.py, tests/js/unsupported_components.test.js, tests/js/local_lm_descent.test.js, harness stubs, CLAUDE.md, plan §8). Read-only. Be adversarial. Findings ranked BLOCKER / MAJOR / MINOR with file:line and a concrete failing scenario; then VERDICT: GO or VERDICT: NO-GO. Budget your time: a verdict is required within the run.

READ FIRST: docs/superpowers/plans/2026-09-21-scattered-starts-and-unsupported-components.md (§1, §3.4, §3.5 site table, §5 decisions, §6 measurement, §8 as built). Owner decisions are not up for review; judge whether the code honours them:
 - a component whose amplitude the fit drove to its floor / pinned on a bound / fitted to residue is an explicit OUTCOME — "not supported by the data" — and its centre, width and sigma are NOT reported anywhere a result is shown, stored or exported;
 - excluded from the Quantify body and LISTED beneath ("0.0 % would be a measurement claim");
 - zero is allowed in both engines (local floor 1 -> 0);
 - the statistic is the Auto-Fit anchor's with/without-component F (F >= 10), NOT an intensity floor — do not propose one; its known limits (single-channel artefact -> false "unsupported"; redundancy under overlap NOT detected — that is step (c)'s refit, out of scope here) are accepted;
 - "the fit did not determine it" is a statement about THIS fit; older saved peaks without a verdict read as not established and display as before.

SITE TABLE — break it (a consumer of centre/width/sigma/area%/At% for a peak that I missed, or a site that now shows something wrong):
 Server: run_fit per-peak block (support computed with the fit's weights, fitted_sub, comp curve; n_free of the component; nvarys); linked components resolved after the loop.
 Page: _componentSupportFromResponse (twin) / _applySupport / _isUnsupported / _unsupportedBadge; applyBackendResult (writes p.support first); runFitLocal commit (null); runPropagation clone (null); renderPeakList; renderResults (+ percentages over supported area; note); _validateUncertainties rule 0; renderQuantify / recalcQuantify; updatePlot dataset label; _buildStackDatasets label; _doPublicationExport (no label at the component's maximum; legend); exportFitTable CSV/XLSX (Status column, empty cells, At% '', WARNING); exportResults TSV header; saves persist p.support because peaks are spread whole (_doSaveFit, _doSaveSpectrum, buildTabData) and loads spread them back; runFitLocal clamp amplitude floor 0.
 NOT touched on purpose: Find Peaks' own table (engine response; autofit/engine already treats amplitude@min as absent); the Auto-Fit anchor check (unchanged; step (c) adds the refit); charge correction (covered by that check); history list / batch summary / status bar (show no per-peak numbers — verify).

QUESTIONS
 a. Consumers I missed: grep for p.center / p.fwhm / stderr / area% / At% rendering or export of a peak; the stack-tab entry render paths (3 paths in _buildEntryRenderData) and the stack legend; the fit-history preview; the figure's data columns; anything that derives a global from a component's centre (charge reference is decorated only — verify); _fpPeakFromBackend's `amplitude || 1` and buildAutoFitModel's Math.max(1, ...) seeds — should they change with the floor, and does leaving them cause harm?
 b. Staleness: p.support after the student edits the peak (moves it, changes amplitude, unlocks it) without refitting — the badge and the suppressed cells then describe a DIFFERENT model. Compare with how step (a) bound its evidence to the fit (startsModelKey). Is a per-peak verdict that survives edits acceptable (it is a property of the last fit, like _backendParams and the stderr map) or must it be invalidated / labelled? Undo/redo and history restore carry it with the peak.
 c. Linked components: `follows` resolution when the parent is itself unsupported / when ids are strings vs numbers / when the parent is absent from the response; the page's _applySupport uses the parent's raw response entry — consistent with the server?
 d. The local engine at floor 0: any regression in runFitLocal (paramScale floors at 1; active-set logic; certificate) for a model with an amplitude at 0 — the rewritten tests in local_lm_descent.test.js pin wall behaviour with negative data; is that sufficient, and are there other tests or code that assumed the floor of 1 (scanner script, docs, parity harness)?
 e. Percentages: the Results table and the sidebar now compute area % over SUPPORTED components; the CSV At% likewise; the TSV keeps the zero column. Any inconsistency between sites, or with Quantify's own total?
 f. The twin: divergence from the server for a response WITH the support field (it defers) and WITHOUT (loaded files: counts/fitted_y/y present?) — do saved spectra / projects actually carry those arrays for the twin to use, or is the twin dead code in practice? Say which.
 g. Wording: anything that certifies or blames the student; anything that says "zero amplitude" where the statistic says "not supported".

Finish with VERDICT: GO or VERDICT: NO-GO.
