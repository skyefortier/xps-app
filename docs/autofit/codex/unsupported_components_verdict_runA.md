No BLOCKER found. Five MAJOR findings prevent approval.

1. **MAJOR — Local fits erase the zero-amplitude outcome.** [templates/index.html:8085](templates/index.html:8085) sets every committed peak’s support to `null`. Concrete case: the rewritten negative-data test converges with amplitude exactly zero; Results still reports its centre/width, and Quantify includes its row with `0.0 %`. The local starting-point warning does not implement the owner’s explicit unsupported outcome. The local fit has enough data to evaluate the same removal statistic.

2. **MAJOR — The scattered-starts table bypasses suppression.** [templates/index.html:7385](templates/index.html:7385) unconditionally prints every component’s area percentage and centre displacement, including the “Your fit” row and largest-move column. With an unsupported component and any alternative present, the main Results table suppresses it while the comparison below reports, for example, `0.3 %` and `+1.40 eV`. Its percentages also retain the unsupported area in their denominator. Reproduced with the extracted renderer. Alternatives need their own support evidence; the current fit’s verdict cannot simply be copied across.

3. **MAJOR — CSV/XLSX still exports undetermined width parameters.** [templates/index.html:10958](templates/index.html:10958) blanks FWHM but retains `sc.beta` and `sc.m`. Reproduced an unsupported DS+G export containing `Beta = 0.3` and `M_Gauss = 0.4`: these are its Lorentzian half-width and Gaussian FWHM. Suppression must cover the active lineshape’s width parameters.

4. **MAJOR — Support survives changes to the model it describes.** [templates/index.html:6935](templates/index.html:6935) trusts `p.support` without an evidence key; edits at [templates/index.html:5762](templates/index.html:5762) retain it. Reproduced moving an unsupported peak to 289 eV and increasing amplitude to 10,000: it still carries `F = 2.01, supported: false`, so the edited component remains excluded from quantification and exports. Keeping historical evidence is reasonable; presenting it unqualified against current parameters is not. Bind it to the fitted model/context or explicitly distinguish last-fit evidence. Changing another component can also invalidate this conditional statistic.

5. **MAJOR — Linked support resolution depends on response order.** [fitting.py:1748](fitting.py:1748) makes one forward pass. For request order `[root, grandchild, child]`, with `grandchild → child → root`, the grandchild reads the child’s preliminary verdict. An actual `run_fit` reproduction returned root unsupported, child unsupported, **grandchild supported**. The page can mask this particular three-level case by reading the parent’s final response entry, leaving server and page inconsistent; its own raw-entry lookup is not recursive either. Resolve complete ancestry independently of order.

6. **MINOR — Wording claims more than the statistic establishes.** [templates/index.html:6904](templates/index.html:6904) asserts “intensity is at or near zero”; [templates/index.html:8275](templates/index.html:8275) says removal “does not make the fit worse.” A component with positive `delta_chi2` and `0 < F < 10` does worsen the fit, below the chosen significance threshold. The plural wording also implies a joint-removal test that was never performed. Use the conditional, per-component statistical statement; the accepted artefact limitation makes the intensity assertion particularly unsafe.

Other requested checks:

- **Twin:** correct raw-count weighting and server-field deferral. It is effectively unused for ordinary saved-file loading: loaders never call `_applySupport`, and persisted arrays use different structures/names. It remains useful for an older backend response lacking support. Older saves display as before, consistent with the accepted decision.
- **Links:** ordinary one-level links tolerate numeric/string IDs in support lookup. A missing parent response leaves the child’s own verdict or `null`; no successful server response should omit its parent.
- **Local floor:** no numerical regression found. Additional executions starting at amplitude **zero**, with fixed and free centre/width, recovered amplitude 10. The existing “moves off the wall” test still starts at **1**, so it no longer tests the wall. The rewritten negative-data width test becomes flat once amplitude reaches zero.
- **Consumers:** all three stack render paths retain peak support; dataset labels use it. Stack legend, history list, history preview, batch summary and status bar expose no additional per-component parameter table. Charge-reference decoration does not derive correction.
- **Percentages/curves:** main Results/sidebar and Quantify/CSV consistently exclude unsupported components, accounting for RSF differences. TSV and figure curves retain actual component values—unsupported does **not** guarantee a zero curve.
- **Seeds:** Auto-Fit’s positive starting amplitudes need no floor-related change. `_fpPeakFromBackend` still converts literal zero to one, but that is an existing Find Peaks application issue, not a demonstrated regression here.

Validation: **77/78 JavaScript tests passed**; the parity test was blocked by the read-only temporary-directory restriction. **Five Python tests passed**; the upload/API test requiring temporary files was excluded. No files changed.

**VERDICT: NO-GO.**
