- **MAJOR — DS+G widths leak into CSV/XLSX.** [templates/index.html:10958](/Users/skyefortier/xps-app/.claude/worktrees/feature-unsupported-components/templates/index.html:10958) blanks FWHM but retains `sc.beta` and `sc.m`. Reproduced an unsupported DS+G export containing `Beta=0.3` and `M_Gauss=0.4`: those are its Lorentzian half-width and Gaussian FWHM. Suppress these width columns too.

- **MAJOR — Scattered-starts comparison is a missed consumer.** [templates/index.html:7385](/Users/skyefortier/xps-app/.claude/worktrees/feature-unsupported-components/templates/index.html:7385) unconditionally renders every component’s percentage and centre displacement. With an unsupported component and an alternative present, I reproduced “Your fit” reporting `0.2` and `+1.20 eV`, also naming that component as the largest move. The main Results row suppresses its position while this table reports it; its percentages also retain the old denominator. Apply support semantics to each solution’s presentation.

- **MAJOR — Support survives changes to the model without qualification.** [templates/index.html:6934](/Users/skyefortier/xps-app/.claude/worktrees/feature-unsupported-components/templates/index.html:6934), [templates/index.html:5754](/Users/skyefortier/xps-app/.claude/worktrees/feature-unsupported-components/templates/index.html:5754). After changing an unsupported peak’s amplitude from 215 to 90,000, its original `F=2.01, supported=false` remains. Quantify and exports then exclude the edited component under evidence from another model. Retaining historical evidence is reasonable; applying it unconditionally to current values is not. Bind it to the complete fitted model/context, as `startsModelKey` does, or explicitly distinguish the last fit from edited parameters. Other-component edits matter too because the statistic holds those components fixed.

- **MAJOR — Local zero-amplitude outcomes remain unclassified.** [templates/index.html:8085](/Users/skyefortier/xps-app/.claude/worktrees/feature-unsupported-components/templates/index.html:8085) always clears support. The new negative-data wall test converges at amplitude zero, but that peak subsequently reports centre/width and occupies a Quantify row with `0.0%`. The local starting-point warning does not supply the required component-specific outcome. Clearing previous support is necessary; stopping there leaves the owner’s zero-floor outcome incomplete.

- **MAJOR — Linked support depends on response order; browser and server disagree.** [fitting.py:1748](/Users/skyefortier/xps-app/.claude/worktrees/feature-unsupported-components/fitting.py:1748), [templates/index.html:6930](/Users/skyefortier/xps-app/.claude/worktrees/feature-unsupported-components/templates/index.html:6930). Reproduced a successful fit with request order `[3,2,1]`, links `3→2→1`, and a supported root. Server returned support `[false,true,true]`: peak 3 copied peak 2’s unresolved verdict. The page subsequently reads peak 2’s resolved response entry and changes peak 3 to supported. Resolve ancestry recursively or topologically before returning the response.

- **MINOR — The stationarity oracle still uses floor 1.** [tests/js/local_lm_descent.test.js:269](/Users/skyefortier/xps-app/.claude/worktrees/feature-unsupported-components/tests/js/local_lm_descent.test.js:269). `BOX.amplitude` remains `[1, Infinity]`. I demonstrated that it accepts amplitude zero against a positive Gaussian of amplitude 0.1: both nominal small probes jump to 1 and worsen the objective, masking feasible descent near zero. Update the oracle and retain a recovery-from-zero case. The shipped solver recovered 0.1 and 10 from zero in my probes.

- **MINOR — Wording asserts more than the statistic establishes.** [templates/index.html:6904](/Users/skyefortier/xps-app/.claude/worktrees/feature-unsupported-components/templates/index.html:6904), [templates/index.html:11174](/Users/skyefortier/xps-app/.claude/worktrees/feature-unsupported-components/templates/index.html:11174). An unsupported component can have positive removal cost, and the accepted artefact limitation can flag a substantial component. “Does not make the fit worse” and “its intensity is at or near zero” are therefore inaccurate. Say “does not make the fit significantly worse under this test.” I found no student-blaming or certification wording in the additions.

- **MINOR — Quantify reports 100% when nothing is quantified.** [templates/index.html:8389](/Users/skyefortier/xps-app/.claude/worktrees/feature-unsupported-components/templates/index.html:8389). When every component is unsupported, the body is empty and normalized total is zero, but the hardcoded percentage total remains `100%`. Show an unavailable total.

Other requested checks:

- All three stack render paths preserve the peak object and support label. History preview draws only the combined envelope; history list, stack sidebar legend, batch summary and status bar introduce no per-component parameter reporting. `isChargeReference` is decorative.
- Ordinary Results/sidebar percentages and CSV/Quantify normalization agree about excluding unsupported components. TSV retains actual component curves, which can be **nonzero**, with annotated headers.
- The twin defers to server support. It is effectively unused for normal saved-file loading: loaders do not call it, and saved spectra/projects do not preserve its response-shaped `counts/fitted_y/individual_peaks[].y` inputs. Older peaks therefore display as before, consistent with the accepted decision.
- Single-level linked ID matching normalizes strings/numbers. Missing parents are rejected during server model construction; the page retains the child’s own verdict when parent evidence is absent.
- Auto-Fit’s positive seeds do not impose a fitted floor. `_fpPeakFromBackend` still converts literal zero to 1; that is an existing Find Peaks conversion, not a demonstrated new local-solver regression. Scanner/parity code revealed no additional floor-1 dependency.

Validation: 77 focused JS tests passed; one Python-backed parity test was blocked by temporary-file restrictions. Five Python support tests passed with capture/cache disabled; the upload/API test was excluded. Workspace unchanged.

**VERDICT: NO-GO**
