# Codex adversarial CODE review — unit W1 (local-engine Poisson weighting) — round 1, RUN A (2026-09-18)

codex exec, read-only sandbox, model_reasoning_effort=high, gtimeout rails. tokens used.
Prompt: docs/autofit/codex/w1_local_weighting_review_prompt.txt
Outcome: NO-GO x2. Site table (enumerated up front) held: no omitted consumer; one LISTED site (TSV) used the constant instead of the objective-aware helper. Also: reduced chi-square counted the never-optimised caM; the follow-up note over-generalised (local amplitude floor 1 vs server 0 can still give >100 % area gaps); minors on upload rounding and CPS. Dispositioned in w1_local_weighting_recheck_prompt.txt.

1. **MAJOR — TSV exports mislabel legacy unweighted fits.** [exportResults](/Users/skyefortier/xps-app/.claude/worktrees/feature-local-lm-poisson-weighting/templates/index.html:10065) uses `_LOCAL_FIT_CAVEAT` directly. After W1, exporting an A0 result produces “Poisson-weighted like the server.” This also affects legacy models carried through `.fit.json`, undo or batch copying. Use `_localFitCaveat` with the governing result/provenance. The site table includes TSV, but its claim of objective-aware coverage is false. **No additional active consumer omitted from the table was found.**

2. **MAJOR — Reduced χ² counts permanently held `caM` as varied.** [The denominator](/Users/skyefortier/xps-app/.claude/worktrees/feature-local-lm-poisson-weighting/templates/index.html:7674) subtracts `params.length`, including `caM`, although the engine excludes it from optimisation. Reproduction: a 21-point LACX amplitude fit gives identical parameters and χ² = 9.91196837 with either `fixCaM` setting, but reports χ²ᵣ = **0.49559842 versus 0.52168255**—a 5.26% change without changing the fit. Exclude permanently held parameters from the local varied count; retain continuous parameters temporarily blocked at bounds. W1’s new statistic claim exposes this inherited counting error.

3. **MAJOR — The student note makes an unsupported universal correction.** [Lines 20–26](/Users/skyefortier/xps-app/.claude/worktrees/feature-local-lm-poisson-weighting/docs/comms/2026-09-18-batch-fit-weighting-followup-note.md:20) say differences above 100% no longer apply; lines 55–58 generalise GL agreement and attribute large movements mainly to multiple solutions. Counterexample: one GL peak, fixed centre/width/mix, true amplitude **0.4**, started at 5. Both engines converge, but local returns **1** and server **0.4**, giving **150% area disagreement** because their amplitude bounds differ. This is a single-variable convex problem. Restrict the correction to the measured targets and acknowledge remaining constraint differences. Also, “within 0.3 percentage points” understates the plan’s measured **0.32 pp**.

4. **MINOR — Weights match the server formula, but are not numerically identical on every page input.** [uploadToBackend](/Users/skyefortier/xps-app/.claude/worktrees/feature-local-lm-poisson-weighting/templates/index.html:6171) rounds intensity to two decimals; local fitting retains full precision. For intensity 1.0049, local weight is **0.99755897**, server weight **1**. The new parity bridge bypasses this serialization. Qualify “identical” or test the actual upload representation.

5. **MINOR — CPS inputs do not justify an unconditional counting-noise interpretation.** [The new wording](/Users/skyefortier/xps-app/.claude/worktrees/feature-local-lm-poisson-weighting/docs/comms/2026-09-18-batch-fit-weighting-followup-note.md:20) promises weighting by counting noise and a “real reduced χ².” Both engines accept CPS without exposure conversion. Matching the server’s intensity-weighting convention does not establish calibrated Poisson uncertainties for rates; that requires acquisition-time information.

Both callers correctly reconstruct input intensity, including manual backgrounds; display toggles do not affect it. Negative/sub-unit flooring, unweighted RMSE, R-factor calculation, scanner metadata classification and A0 acceptance/certificate behavior checked out.

Validation: **244 JS tests passed, 3 TODO**, using an in-memory setup accommodating read-only Python imports. Scanner metadata checks passed; filesystem-based pytest tests were blocked by the read-only sandbox.

VERDICT: NO-GO — Legacy TSV provenance is misstated, permanently held parameters distort reduced χ², and the student correction exceeds the measured evidence.
